// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteCleanupExpiredArgs {
    pub ticket_id: String,
    pub now_arg: String,
    pub batch_size: u32,
}

impl From<TicketremoteCleanupExpiredArgs> for super::Reducer {
    fn from(args: TicketremoteCleanupExpiredArgs) -> Self {
        Self::TicketremoteCleanupExpired {
            ticket_id: args.ticket_id,
            now_arg: args.now_arg,
            batch_size: args.batch_size,
        }
    }
}

impl __sdk::InModule for TicketremoteCleanupExpiredArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_cleanup_expired`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_cleanup_expired {
    /// Request that the remote module invoke the reducer `ticketremote_cleanup_expired` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_cleanup_expired:ticketremote_cleanup_expired_then`] to run a callback after the reducer completes.
    fn ticketremote_cleanup_expired(
        &self,
        ticket_id: String,
        now_arg: String,
        batch_size: u32,
    ) -> __sdk::Result<()> {
        self.ticketremote_cleanup_expired_then(ticket_id, now_arg, batch_size, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `ticketremote_cleanup_expired` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_cleanup_expired_then(
        &self,
        ticket_id: String,
        now_arg: String,
        batch_size: u32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_cleanup_expired for super::RemoteReducers {
    fn ticketremote_cleanup_expired_then(
        &self,
        ticket_id: String,
        now_arg: String,
        batch_size: u32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteCleanupExpiredArgs {
                ticket_id,
                now_arg,
                batch_size,
            },
            callback,
        )
    }
}
