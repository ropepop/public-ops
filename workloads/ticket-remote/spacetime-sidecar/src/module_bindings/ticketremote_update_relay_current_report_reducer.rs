// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteUpdateRelayCurrentReportArgs {
    pub ticket_id: String,
    pub backend_id: String,
    pub video_clients: u32,
    pub stream_verdict: String,
    pub last_frame_ago_millis: u32,
    pub frames_forwarded: String,
    pub status_json: String,
    pub now_arg: String,
}

impl From<TicketremoteUpdateRelayCurrentReportArgs> for super::Reducer {
    fn from(args: TicketremoteUpdateRelayCurrentReportArgs) -> Self {
        Self::TicketremoteUpdateRelayCurrentReport {
            ticket_id: args.ticket_id,
            backend_id: args.backend_id,
            video_clients: args.video_clients,
            stream_verdict: args.stream_verdict,
            last_frame_ago_millis: args.last_frame_ago_millis,
            frames_forwarded: args.frames_forwarded,
            status_json: args.status_json,
            now_arg: args.now_arg,
        }
    }
}

impl __sdk::InModule for TicketremoteUpdateRelayCurrentReportArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_update_relay_current_report`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_update_relay_current_report {
    /// Request that the remote module invoke the reducer `ticketremote_update_relay_current_report` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_update_relay_current_report:ticketremote_update_relay_current_report_then`] to run a callback after the reducer completes.
    fn ticketremote_update_relay_current_report(
        &self,
        ticket_id: String,
        backend_id: String,
        video_clients: u32,
        stream_verdict: String,
        last_frame_ago_millis: u32,
        frames_forwarded: String,
        status_json: String,
        now_arg: String,
    ) -> __sdk::Result<()> {
        self.ticketremote_update_relay_current_report_then(
            ticket_id,
            backend_id,
            video_clients,
            stream_verdict,
            last_frame_ago_millis,
            frames_forwarded,
            status_json,
            now_arg,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `ticketremote_update_relay_current_report` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_update_relay_current_report_then(
        &self,
        ticket_id: String,
        backend_id: String,
        video_clients: u32,
        stream_verdict: String,
        last_frame_ago_millis: u32,
        frames_forwarded: String,
        status_json: String,
        now_arg: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_update_relay_current_report for super::RemoteReducers {
    fn ticketremote_update_relay_current_report_then(
        &self,
        ticket_id: String,
        backend_id: String,
        video_clients: u32,
        stream_verdict: String,
        last_frame_ago_millis: u32,
        frames_forwarded: String,
        status_json: String,
        now_arg: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteUpdateRelayCurrentReportArgs {
                ticket_id,
                backend_id,
                video_clients,
                stream_verdict,
                last_frame_ago_millis,
                frames_forwarded,
                status_json,
                now_arg,
            },
            callback,
        )
    }
}
