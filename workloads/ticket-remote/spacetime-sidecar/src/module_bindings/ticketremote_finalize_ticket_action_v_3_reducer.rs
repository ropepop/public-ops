// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteFinalizeTicketActionV3Args {
    pub ticket_id: String,
    pub backend_id: String,
    pub command_id: String,
    pub action_id: String,
    pub target: String,
    pub status: String,
    pub phase: String,
    pub current_view: String,
    pub stream_epoch: String,
    pub frame_sequence: String,
    pub reason: String,
    pub completed_at: String,
    pub attempt_id: String,
    pub interaction_revision: String,
    pub activation_revision: String,
    pub has_slider_region: bool,
    pub left_basis_points: u32,
    pub top_basis_points: u32,
    pub right_basis_points: u32,
    pub bottom_basis_points: u32,
    pub now_arg: String,
}

impl From<TicketremoteFinalizeTicketActionV3Args> for super::Reducer {
    fn from(args: TicketremoteFinalizeTicketActionV3Args) -> Self {
        Self::TicketremoteFinalizeTicketActionV3 {
            ticket_id: args.ticket_id,
            backend_id: args.backend_id,
            command_id: args.command_id,
            action_id: args.action_id,
            target: args.target,
            status: args.status,
            phase: args.phase,
            current_view: args.current_view,
            stream_epoch: args.stream_epoch,
            frame_sequence: args.frame_sequence,
            reason: args.reason,
            completed_at: args.completed_at,
            attempt_id: args.attempt_id,
            interaction_revision: args.interaction_revision,
            activation_revision: args.activation_revision,
            has_slider_region: args.has_slider_region,
            left_basis_points: args.left_basis_points,
            top_basis_points: args.top_basis_points,
            right_basis_points: args.right_basis_points,
            bottom_basis_points: args.bottom_basis_points,
            now_arg: args.now_arg,
        }
    }
}

impl __sdk::InModule for TicketremoteFinalizeTicketActionV3Args {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_finalize_ticket_action_v3`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_finalize_ticket_action_v_3 {
    /// Request that the remote module invoke the reducer `ticketremote_finalize_ticket_action_v3` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_finalize_ticket_action_v_3:ticketremote_finalize_ticket_action_v_3_then`] to run a callback after the reducer completes.
    fn ticketremote_finalize_ticket_action_v_3(
        &self,
        ticket_id: String,
        backend_id: String,
        command_id: String,
        action_id: String,
        target: String,
        status: String,
        phase: String,
        current_view: String,
        stream_epoch: String,
        frame_sequence: String,
        reason: String,
        completed_at: String,
        attempt_id: String,
        interaction_revision: String,
        activation_revision: String,
        has_slider_region: bool,
        left_basis_points: u32,
        top_basis_points: u32,
        right_basis_points: u32,
        bottom_basis_points: u32,
        now_arg: String,
    ) -> __sdk::Result<()> {
        self.ticketremote_finalize_ticket_action_v_3_then(
            ticket_id,
            backend_id,
            command_id,
            action_id,
            target,
            status,
            phase,
            current_view,
            stream_epoch,
            frame_sequence,
            reason,
            completed_at,
            attempt_id,
            interaction_revision,
            activation_revision,
            has_slider_region,
            left_basis_points,
            top_basis_points,
            right_basis_points,
            bottom_basis_points,
            now_arg,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `ticketremote_finalize_ticket_action_v3` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_finalize_ticket_action_v_3_then(
        &self,
        ticket_id: String,
        backend_id: String,
        command_id: String,
        action_id: String,
        target: String,
        status: String,
        phase: String,
        current_view: String,
        stream_epoch: String,
        frame_sequence: String,
        reason: String,
        completed_at: String,
        attempt_id: String,
        interaction_revision: String,
        activation_revision: String,
        has_slider_region: bool,
        left_basis_points: u32,
        top_basis_points: u32,
        right_basis_points: u32,
        bottom_basis_points: u32,
        now_arg: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_finalize_ticket_action_v_3 for super::RemoteReducers {
    fn ticketremote_finalize_ticket_action_v_3_then(
        &self,
        ticket_id: String,
        backend_id: String,
        command_id: String,
        action_id: String,
        target: String,
        status: String,
        phase: String,
        current_view: String,
        stream_epoch: String,
        frame_sequence: String,
        reason: String,
        completed_at: String,
        attempt_id: String,
        interaction_revision: String,
        activation_revision: String,
        has_slider_region: bool,
        left_basis_points: u32,
        top_basis_points: u32,
        right_basis_points: u32,
        bottom_basis_points: u32,
        now_arg: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteFinalizeTicketActionV3Args {
                ticket_id,
                backend_id,
                command_id,
                action_id,
                target,
                status,
                phase,
                current_view,
                stream_epoch,
                frame_sequence,
                reason,
                completed_at,
                attempt_id,
                interaction_revision,
                activation_revision,
                has_slider_region,
                left_basis_points,
                top_basis_points,
                right_basis_points,
                bottom_basis_points,
                now_arg,
            },
            callback,
        )
    }
}
