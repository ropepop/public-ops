// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteCommitTicketActivationArgs {
    pub ticket_id: String,
    pub backend_id: String,
    pub attempt_id: String,
    pub interaction_revision: String,
    pub activation_revision: String,
}

impl From<TicketremoteCommitTicketActivationArgs> for super::Reducer {
    fn from(args: TicketremoteCommitTicketActivationArgs) -> Self {
        Self::TicketremoteCommitTicketActivation {
            ticket_id: args.ticket_id,
            backend_id: args.backend_id,
            attempt_id: args.attempt_id,
            interaction_revision: args.interaction_revision,
            activation_revision: args.activation_revision,
        }
    }
}

impl __sdk::InModule for TicketremoteCommitTicketActivationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_commit_ticket_activation`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_commit_ticket_activation {
    /// Request that the remote module invoke the reducer `ticketremote_commit_ticket_activation` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_commit_ticket_activation:ticketremote_commit_ticket_activation_then`] to run a callback after the reducer completes.
    fn ticketremote_commit_ticket_activation(
        &self,
        ticket_id: String,
        backend_id: String,
        attempt_id: String,
        interaction_revision: String,
        activation_revision: String,
    ) -> __sdk::Result<()> {
        self.ticketremote_commit_ticket_activation_then(
            ticket_id,
            backend_id,
            attempt_id,
            interaction_revision,
            activation_revision,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `ticketremote_commit_ticket_activation` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_commit_ticket_activation_then(
        &self,
        ticket_id: String,
        backend_id: String,
        attempt_id: String,
        interaction_revision: String,
        activation_revision: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_commit_ticket_activation for super::RemoteReducers {
    fn ticketremote_commit_ticket_activation_then(
        &self,
        ticket_id: String,
        backend_id: String,
        attempt_id: String,
        interaction_revision: String,
        activation_revision: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteCommitTicketActivationArgs {
                ticket_id,
                backend_id,
                attempt_id,
                interaction_revision,
                activation_revision,
            },
            callback,
        )
    }
}
