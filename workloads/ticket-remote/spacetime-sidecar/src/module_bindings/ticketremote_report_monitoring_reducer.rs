// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteReportMonitoringArgs {
    pub ticket_id: String,
    pub backend_id: String,
    pub epoch: String,
    pub session_id: String,
    pub sequence: u64,
    pub status: String,
    pub reason: String,
    pub observed_at_ms: i64,
}

impl From<TicketremoteReportMonitoringArgs> for super::Reducer {
    fn from(args: TicketremoteReportMonitoringArgs) -> Self {
        Self::TicketremoteReportMonitoring {
            ticket_id: args.ticket_id,
            backend_id: args.backend_id,
            epoch: args.epoch,
            session_id: args.session_id,
            sequence: args.sequence,
            status: args.status,
            reason: args.reason,
            observed_at_ms: args.observed_at_ms,
        }
    }
}

impl __sdk::InModule for TicketremoteReportMonitoringArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_report_monitoring`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_report_monitoring {
    /// Request that the remote module invoke the reducer `ticketremote_report_monitoring` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_report_monitoring:ticketremote_report_monitoring_then`] to run a callback after the reducer completes.
    fn ticketremote_report_monitoring(
        &self,
        ticket_id: String,
        backend_id: String,
        epoch: String,
        session_id: String,
        sequence: u64,
        status: String,
        reason: String,
        observed_at_ms: i64,
    ) -> __sdk::Result<()> {
        self.ticketremote_report_monitoring_then(
            ticket_id,
            backend_id,
            epoch,
            session_id,
            sequence,
            status,
            reason,
            observed_at_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `ticketremote_report_monitoring` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_report_monitoring_then(
        &self,
        ticket_id: String,
        backend_id: String,
        epoch: String,
        session_id: String,
        sequence: u64,
        status: String,
        reason: String,
        observed_at_ms: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_report_monitoring for super::RemoteReducers {
    fn ticketremote_report_monitoring_then(
        &self,
        ticket_id: String,
        backend_id: String,
        epoch: String,
        session_id: String,
        sequence: u64,
        status: String,
        reason: String,
        observed_at_ms: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteReportMonitoringArgs {
                ticket_id,
                backend_id,
                epoch,
                session_id,
                sequence,
                status,
                reason,
                observed_at_ms,
            },
            callback,
        )
    }
}
