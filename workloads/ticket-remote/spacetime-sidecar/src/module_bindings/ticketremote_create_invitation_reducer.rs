// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteCreateInvitationArgs {
    pub ticket_id: String,
    pub invitation_id: String,
    pub token_hash: String,
    pub label: String,
    pub actor_email: String,
    pub duration_minutes: u32,
    pub stream_minutes: u32,
}

impl From<TicketremoteCreateInvitationArgs> for super::Reducer {
    fn from(args: TicketremoteCreateInvitationArgs) -> Self {
        Self::TicketremoteCreateInvitation {
            ticket_id: args.ticket_id,
            invitation_id: args.invitation_id,
            token_hash: args.token_hash,
            label: args.label,
            actor_email: args.actor_email,
            duration_minutes: args.duration_minutes,
            stream_minutes: args.stream_minutes,
        }
    }
}

impl __sdk::InModule for TicketremoteCreateInvitationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_create_invitation`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_create_invitation {
    /// Request that the remote module invoke the reducer `ticketremote_create_invitation` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_create_invitation:ticketremote_create_invitation_then`] to run a callback after the reducer completes.
    fn ticketremote_create_invitation(
        &self,
        ticket_id: String,
        invitation_id: String,
        token_hash: String,
        label: String,
        actor_email: String,
        duration_minutes: u32,
        stream_minutes: u32,
    ) -> __sdk::Result<()> {
        self.ticketremote_create_invitation_then(
            ticket_id,
            invitation_id,
            token_hash,
            label,
            actor_email,
            duration_minutes,
            stream_minutes,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `ticketremote_create_invitation` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_create_invitation_then(
        &self,
        ticket_id: String,
        invitation_id: String,
        token_hash: String,
        label: String,
        actor_email: String,
        duration_minutes: u32,
        stream_minutes: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_create_invitation for super::RemoteReducers {
    fn ticketremote_create_invitation_then(
        &self,
        ticket_id: String,
        invitation_id: String,
        token_hash: String,
        label: String,
        actor_email: String,
        duration_minutes: u32,
        stream_minutes: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteCreateInvitationArgs {
                ticket_id,
                invitation_id,
                token_hash,
                label,
                actor_email,
                duration_minutes,
                stream_minutes,
            },
            callback,
        )
    }
}
