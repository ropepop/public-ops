// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteUpdateTicketActionV3Args {
    pub ticket_id: String,
    pub backend_id: String,
    pub action_id: String,
    pub target: String,
    pub status: String,
    pub phase: String,
    pub current_view: String,
    pub switch_available: bool,
    pub switch_expires_at: String,
    pub stream_epoch: String,
    pub frame_sequence: String,
    pub reason: String,
    pub completed_at: String,
    pub now_arg: String,
}

impl From<TicketremoteUpdateTicketActionV3Args> for super::Reducer {
    fn from(args: TicketremoteUpdateTicketActionV3Args) -> Self {
        Self::TicketremoteUpdateTicketActionV3 {
            ticket_id: args.ticket_id,
            backend_id: args.backend_id,
            action_id: args.action_id,
            target: args.target,
            status: args.status,
            phase: args.phase,
            current_view: args.current_view,
            switch_available: args.switch_available,
            switch_expires_at: args.switch_expires_at,
            stream_epoch: args.stream_epoch,
            frame_sequence: args.frame_sequence,
            reason: args.reason,
            completed_at: args.completed_at,
            now_arg: args.now_arg,
        }
    }
}

impl __sdk::InModule for TicketremoteUpdateTicketActionV3Args {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_update_ticket_action_v3`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_update_ticket_action_v_3 {
    /// Request that the remote module invoke the reducer `ticketremote_update_ticket_action_v3` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_update_ticket_action_v_3:ticketremote_update_ticket_action_v_3_then`] to run a callback after the reducer completes.
    fn ticketremote_update_ticket_action_v_3(
        &self,
        ticket_id: String,
        backend_id: String,
        action_id: String,
        target: String,
        status: String,
        phase: String,
        current_view: String,
        switch_available: bool,
        switch_expires_at: String,
        stream_epoch: String,
        frame_sequence: String,
        reason: String,
        completed_at: String,
        now_arg: String,
    ) -> __sdk::Result<()> {
        self.ticketremote_update_ticket_action_v_3_then(
            ticket_id,
            backend_id,
            action_id,
            target,
            status,
            phase,
            current_view,
            switch_available,
            switch_expires_at,
            stream_epoch,
            frame_sequence,
            reason,
            completed_at,
            now_arg,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `ticketremote_update_ticket_action_v3` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_update_ticket_action_v_3_then(
        &self,
        ticket_id: String,
        backend_id: String,
        action_id: String,
        target: String,
        status: String,
        phase: String,
        current_view: String,
        switch_available: bool,
        switch_expires_at: String,
        stream_epoch: String,
        frame_sequence: String,
        reason: String,
        completed_at: String,
        now_arg: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_update_ticket_action_v_3 for super::RemoteReducers {
    fn ticketremote_update_ticket_action_v_3_then(
        &self,
        ticket_id: String,
        backend_id: String,
        action_id: String,
        target: String,
        status: String,
        phase: String,
        current_view: String,
        switch_available: bool,
        switch_expires_at: String,
        stream_epoch: String,
        frame_sequence: String,
        reason: String,
        completed_at: String,
        now_arg: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteUpdateTicketActionV3Args {
                ticket_id,
                backend_id,
                action_id,
                target,
                status,
                phase,
                current_view,
                switch_available,
                switch_expires_at,
                stream_epoch,
                frame_sequence,
                reason,
                completed_at,
                now_arg,
            },
            callback,
        )
    }
}
