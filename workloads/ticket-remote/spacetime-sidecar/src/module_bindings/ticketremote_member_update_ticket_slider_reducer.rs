// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteMemberUpdateTicketSliderArgs {
    pub ticket_id: String,
    pub backend_id: String,
    pub interaction_revision: String,
    pub control_id: String,
    pub input_sequence: String,
    pub input_phase: String,
    pub progress: u32,
}

impl From<TicketremoteMemberUpdateTicketSliderArgs> for super::Reducer {
    fn from(args: TicketremoteMemberUpdateTicketSliderArgs) -> Self {
        Self::TicketremoteMemberUpdateTicketSlider {
            ticket_id: args.ticket_id,
            backend_id: args.backend_id,
            interaction_revision: args.interaction_revision,
            control_id: args.control_id,
            input_sequence: args.input_sequence,
            input_phase: args.input_phase,
            progress: args.progress,
        }
    }
}

impl __sdk::InModule for TicketremoteMemberUpdateTicketSliderArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_member_update_ticket_slider`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_member_update_ticket_slider {
    /// Request that the remote module invoke the reducer `ticketremote_member_update_ticket_slider` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_member_update_ticket_slider:ticketremote_member_update_ticket_slider_then`] to run a callback after the reducer completes.
    fn ticketremote_member_update_ticket_slider(
        &self,
        ticket_id: String,
        backend_id: String,
        interaction_revision: String,
        control_id: String,
        input_sequence: String,
        input_phase: String,
        progress: u32,
    ) -> __sdk::Result<()> {
        self.ticketremote_member_update_ticket_slider_then(
            ticket_id,
            backend_id,
            interaction_revision,
            control_id,
            input_sequence,
            input_phase,
            progress,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `ticketremote_member_update_ticket_slider` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_member_update_ticket_slider_then(
        &self,
        ticket_id: String,
        backend_id: String,
        interaction_revision: String,
        control_id: String,
        input_sequence: String,
        input_phase: String,
        progress: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_member_update_ticket_slider for super::RemoteReducers {
    fn ticketremote_member_update_ticket_slider_then(
        &self,
        ticket_id: String,
        backend_id: String,
        interaction_revision: String,
        control_id: String,
        input_sequence: String,
        input_phase: String,
        progress: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteMemberUpdateTicketSliderArgs {
                ticket_id,
                backend_id,
                interaction_revision,
                control_id,
                input_sequence,
                input_phase,
                progress,
            },
            callback,
        )
    }
}
