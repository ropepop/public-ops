// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteSetStreamDesiredStateArgs {
    pub ticket_id: String,
    pub backend_id: String,
    pub desired_active: bool,
    pub viewer_count: u32,
    pub reason: String,
    pub revision: String,
    pub updated_by: String,
    pub now_arg: String,
}

impl From<TicketremoteSetStreamDesiredStateArgs> for super::Reducer {
    fn from(args: TicketremoteSetStreamDesiredStateArgs) -> Self {
        Self::TicketremoteSetStreamDesiredState {
            ticket_id: args.ticket_id,
            backend_id: args.backend_id,
            desired_active: args.desired_active,
            viewer_count: args.viewer_count,
            reason: args.reason,
            revision: args.revision,
            updated_by: args.updated_by,
            now_arg: args.now_arg,
        }
    }
}

impl __sdk::InModule for TicketremoteSetStreamDesiredStateArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_set_stream_desired_state`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_set_stream_desired_state {
    /// Request that the remote module invoke the reducer `ticketremote_set_stream_desired_state` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_set_stream_desired_state:ticketremote_set_stream_desired_state_then`] to run a callback after the reducer completes.
    fn ticketremote_set_stream_desired_state(
        &self,
        ticket_id: String,
        backend_id: String,
        desired_active: bool,
        viewer_count: u32,
        reason: String,
        revision: String,
        updated_by: String,
        now_arg: String,
    ) -> __sdk::Result<()> {
        self.ticketremote_set_stream_desired_state_then(
            ticket_id,
            backend_id,
            desired_active,
            viewer_count,
            reason,
            revision,
            updated_by,
            now_arg,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `ticketremote_set_stream_desired_state` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_set_stream_desired_state_then(
        &self,
        ticket_id: String,
        backend_id: String,
        desired_active: bool,
        viewer_count: u32,
        reason: String,
        revision: String,
        updated_by: String,
        now_arg: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_set_stream_desired_state for super::RemoteReducers {
    fn ticketremote_set_stream_desired_state_then(
        &self,
        ticket_id: String,
        backend_id: String,
        desired_active: bool,
        viewer_count: u32,
        reason: String,
        revision: String,
        updated_by: String,
        now_arg: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteSetStreamDesiredStateArgs {
                ticket_id,
                backend_id,
                desired_active,
                viewer_count,
                reason,
                revision,
                updated_by,
                now_arg,
            },
            callback,
        )
    }
}
