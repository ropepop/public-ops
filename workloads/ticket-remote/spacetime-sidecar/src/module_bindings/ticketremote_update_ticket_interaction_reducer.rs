// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteUpdateTicketInteractionArgs {
    pub ticket_id: String,
    pub backend_id: String,
    pub status: String,
    pub interaction_revision: String,
    pub activation_revision: String,
    pub activation_at: String,
    pub scheduled_reset_at: String,
    pub reset_request_id: String,
    pub stream_epoch: String,
    pub frame_sequence: String,
    pub phone_display_width: u32,
    pub phone_display_height: u32,
    pub slider_bounds_json: String,
    pub owner_public_id: String,
    pub control_id: String,
    pub lease_phase: String,
    pub lease_expires_at: String,
    pub latest_input_sequence: String,
    pub latest_input_phase: String,
    pub latest_progress: u32,
    pub last_applied_sequence: String,
    pub last_applied_progress: u32,
    pub reason: String,
    pub now_arg: String,
}

impl From<TicketremoteUpdateTicketInteractionArgs> for super::Reducer {
    fn from(args: TicketremoteUpdateTicketInteractionArgs) -> Self {
        Self::TicketremoteUpdateTicketInteraction {
            ticket_id: args.ticket_id,
            backend_id: args.backend_id,
            status: args.status,
            interaction_revision: args.interaction_revision,
            activation_revision: args.activation_revision,
            activation_at: args.activation_at,
            scheduled_reset_at: args.scheduled_reset_at,
            reset_request_id: args.reset_request_id,
            stream_epoch: args.stream_epoch,
            frame_sequence: args.frame_sequence,
            phone_display_width: args.phone_display_width,
            phone_display_height: args.phone_display_height,
            slider_bounds_json: args.slider_bounds_json,
            owner_public_id: args.owner_public_id,
            control_id: args.control_id,
            lease_phase: args.lease_phase,
            lease_expires_at: args.lease_expires_at,
            latest_input_sequence: args.latest_input_sequence,
            latest_input_phase: args.latest_input_phase,
            latest_progress: args.latest_progress,
            last_applied_sequence: args.last_applied_sequence,
            last_applied_progress: args.last_applied_progress,
            reason: args.reason,
            now_arg: args.now_arg,
        }
    }
}

impl __sdk::InModule for TicketremoteUpdateTicketInteractionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_update_ticket_interaction`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_update_ticket_interaction {
    /// Request that the remote module invoke the reducer `ticketremote_update_ticket_interaction` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_update_ticket_interaction:ticketremote_update_ticket_interaction_then`] to run a callback after the reducer completes.
    fn ticketremote_update_ticket_interaction(
        &self,
        ticket_id: String,
        backend_id: String,
        status: String,
        interaction_revision: String,
        activation_revision: String,
        activation_at: String,
        scheduled_reset_at: String,
        reset_request_id: String,
        stream_epoch: String,
        frame_sequence: String,
        phone_display_width: u32,
        phone_display_height: u32,
        slider_bounds_json: String,
        owner_public_id: String,
        control_id: String,
        lease_phase: String,
        lease_expires_at: String,
        latest_input_sequence: String,
        latest_input_phase: String,
        latest_progress: u32,
        last_applied_sequence: String,
        last_applied_progress: u32,
        reason: String,
        now_arg: String,
    ) -> __sdk::Result<()> {
        self.ticketremote_update_ticket_interaction_then(
            ticket_id,
            backend_id,
            status,
            interaction_revision,
            activation_revision,
            activation_at,
            scheduled_reset_at,
            reset_request_id,
            stream_epoch,
            frame_sequence,
            phone_display_width,
            phone_display_height,
            slider_bounds_json,
            owner_public_id,
            control_id,
            lease_phase,
            lease_expires_at,
            latest_input_sequence,
            latest_input_phase,
            latest_progress,
            last_applied_sequence,
            last_applied_progress,
            reason,
            now_arg,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `ticketremote_update_ticket_interaction` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_update_ticket_interaction_then(
        &self,
        ticket_id: String,
        backend_id: String,
        status: String,
        interaction_revision: String,
        activation_revision: String,
        activation_at: String,
        scheduled_reset_at: String,
        reset_request_id: String,
        stream_epoch: String,
        frame_sequence: String,
        phone_display_width: u32,
        phone_display_height: u32,
        slider_bounds_json: String,
        owner_public_id: String,
        control_id: String,
        lease_phase: String,
        lease_expires_at: String,
        latest_input_sequence: String,
        latest_input_phase: String,
        latest_progress: u32,
        last_applied_sequence: String,
        last_applied_progress: u32,
        reason: String,
        now_arg: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_update_ticket_interaction for super::RemoteReducers {
    fn ticketremote_update_ticket_interaction_then(
        &self,
        ticket_id: String,
        backend_id: String,
        status: String,
        interaction_revision: String,
        activation_revision: String,
        activation_at: String,
        scheduled_reset_at: String,
        reset_request_id: String,
        stream_epoch: String,
        frame_sequence: String,
        phone_display_width: u32,
        phone_display_height: u32,
        slider_bounds_json: String,
        owner_public_id: String,
        control_id: String,
        lease_phase: String,
        lease_expires_at: String,
        latest_input_sequence: String,
        latest_input_phase: String,
        latest_progress: u32,
        last_applied_sequence: String,
        last_applied_progress: u32,
        reason: String,
        now_arg: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteUpdateTicketInteractionArgs {
                ticket_id,
                backend_id,
                status,
                interaction_revision,
                activation_revision,
                activation_at,
                scheduled_reset_at,
                reset_request_id,
                stream_epoch,
                frame_sequence,
                phone_display_width,
                phone_display_height,
                slider_bounds_json,
                owner_public_id,
                control_id,
                lease_phase,
                lease_expires_at,
                latest_input_sequence,
                latest_input_phase,
                latest_progress,
                last_applied_sequence,
                last_applied_progress,
                reason,
                now_arg,
            },
            callback,
        )
    }
}
