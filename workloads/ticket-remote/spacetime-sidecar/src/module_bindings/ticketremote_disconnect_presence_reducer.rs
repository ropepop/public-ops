// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TicketremoteDisconnectPresenceArgs {
    pub ticket_id: String,
    pub session_id: String,
    pub now_arg: String,
}

impl From<TicketremoteDisconnectPresenceArgs> for super::Reducer {
    fn from(args: TicketremoteDisconnectPresenceArgs) -> Self {
        Self::TicketremoteDisconnectPresence {
            ticket_id: args.ticket_id,
            session_id: args.session_id,
            now_arg: args.now_arg,
        }
    }
}

impl __sdk::InModule for TicketremoteDisconnectPresenceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ticketremote_disconnect_presence`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ticketremote_disconnect_presence {
    /// Request that the remote module invoke the reducer `ticketremote_disconnect_presence` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ticketremote_disconnect_presence:ticketremote_disconnect_presence_then`] to run a callback after the reducer completes.
    fn ticketremote_disconnect_presence(
        &self,
        ticket_id: String,
        session_id: String,
        now_arg: String,
    ) -> __sdk::Result<()> {
        self.ticketremote_disconnect_presence_then(ticket_id, session_id, now_arg, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `ticketremote_disconnect_presence` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ticketremote_disconnect_presence_then(
        &self,
        ticket_id: String,
        session_id: String,
        now_arg: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl ticketremote_disconnect_presence for super::RemoteReducers {
    fn ticketremote_disconnect_presence_then(
        &self,
        ticket_id: String,
        session_id: String,
        now_arg: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TicketremoteDisconnectPresenceArgs {
                ticket_id,
                session_id,
                now_arg,
            },
            callback,
        )
    }
}
